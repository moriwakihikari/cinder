```
docker-compose build
```
```
docker-compose run --rm next sh -c 'npx create-next-app src --ts --experimental-app --use-npm'
```
```
docker-compose up -d
```
MUI入れる
```
docker-compose run next npm install --save @emotion/react @emotion/styled @mui/icons-material @mui/material
```
